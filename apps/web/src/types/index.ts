// Re-export all shared types from the @rim/types package.
// Components and services in apps/web should import from here,
// not directly from @rim/types, to keep the import path short.
export * from '@rim/types';
